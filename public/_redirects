# Netlify redirects file

# CRITICAL: eBay OAuth callback redirect - MUST BE FIRST
# eBay is configured to redirect to /app/api/ebay/callback-fixed
# But our actual handler is at /.netlify/functions/auth-ebay-callback
/app/api/ebay/callback-fixed  /.netlify/functions/auth-ebay-callback  200!

# Static HTML files - serve directly (including OAuth callback)
/*.html  /:splat.html  200

# API routes
/api/*  /.netlify/functions/:splat  200

# SPA fallback - must be last
/*  /index.html  200